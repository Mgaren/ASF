{% extends 'base.html.twig' %}

{% block title %}HomeSection{% endblock %}

{% block body %}
    <h1>HomeSection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home_section.id }}</td>
            </tr>
            <tr>
                <th>Section</th>
                <td>{{ home_section.sectionSalary.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ home_section.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ home_section.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_homeSection') }}">Retour à la liste</a>

    <a href="{{ path('home_section_edit', {'id': home_section.id}) }}">Editer</a>

    {{ include('home_section/_delete_form.html.twig') }}
{% endblock %}
