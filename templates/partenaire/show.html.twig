{% extends 'base.html.twig' %}

{% block title %}Partenaire{% endblock %}

{% block body %}
    <h1>Partenaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partenaire.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partenaire.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ partenaire.description }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ partenaire.lien }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ partenaire.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_partenaire') }}">Retour à la liste</a>

    <a href="{{ path('partenaire_edit', {'id': partenaire.id}) }}">Editer</a>

    {{ include('partenaire/_delete_form.html.twig') }}
{% endblock %}
