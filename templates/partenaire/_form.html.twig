<div class="wrapper">
    {{ form_start(form) }}
    <div class="form">
        <h1>Ajouter/Editer un Partenaire</h1>
        <h4>* Champs Obligatoires</h4>
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Ajouter') }}</button>
        <a href="{{ path('admin_partenaire') }}">Retour à la liste</a>
    </div>
    {{ form_end(form) }}
</div>
