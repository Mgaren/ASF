<form method="post" action="{{ path('home_carousel_partenaire_delete', {'id': carousel_partenaire.id}) }}" onsubmit="return confirm('Etez-vous sur de vouloir le supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carousel_partenaire.id) }}">
    <button class="btn">Supprimer</button>
</form>
