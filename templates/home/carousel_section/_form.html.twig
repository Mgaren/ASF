<div class="wrapper">
    {{ form_start(form) }}
    <div class="form">
        <h1>Ajouter/Editer le carousel des sections sportives</h1>
        <h4>* Champs Obligatoires</h4>
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Ajouter') }}</button>
        <a href="{{ path('admin_carousel_section') }}">Retour à la liste</a>
    </div>
    {{ form_end(form) }}
</div>
