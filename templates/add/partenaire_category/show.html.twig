{% extends 'base.html.twig' %}

{% block title %}PartenaireCategory{% endblock %}

{% block body %}
    <h1>Catégories pour partenaire adhérent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partenaire_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partenaire_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_partenaire_category') }}">Retour à la liste</a>

    <a href="{{ path('add_partenaire_category_edit', {'id': partenaire_category.id}) }}">Editer</a>

    {{ include('add/partenaire_category/_delete_form.html.twig') }}
{% endblock %}
