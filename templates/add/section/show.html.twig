{% extends 'base.html.twig' %}

{% block title %}SectionSalary{% endblock %}

{% block body %}
    <h1>SectionSalary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ section.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_section') }}">Retour à la liste</a>

    <a href="{{ path('add_section_edit', {'id': section.id}) }}">Editer</a>

    {{ include('add/section/_delete_form.html.twig') }}
{% endblock %}
