<form method="post" action="{{ path('add_section_salary_delete', {'id': section_salary.id}) }}" onsubmit="return confirm('Etez-vous sur de vouloir le supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ section_salary.id) }}">
    <button class="btn">Supprimer</button>
</form>
