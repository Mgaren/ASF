{% extends 'base.html.twig' %}

{% block title %}CguCategory{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cgu_category.id }}</td>
            </tr>
            <tr>
                <th>N° de l'article et son intitulé</th>
                <td>{{ cgu_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_cgu_category') }}">Retour à la liste</a>

    <a href="{{ path('add_cgu_category_edit', {'id': cgu_category.id}) }}">Editer</a>

    {{ include('add/cgu_category/_delete_form.html.twig') }}
{% endblock %}
