{% extends 'base.html.twig' %}

{% block title %}DirigeantsPost{% endblock %}

{% block body %}
    <h1>DirigeantsPost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dirigeants_post.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dirigeants_post.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_dirigeants_post') }}">Retour à la liste</a>

    <a href="{{ path('add_dirigeants_post_edit', {'id': dirigeants_post.id}) }}">Editer</a>

    {{ include('add/post/_delete_form.html.twig') }}
{% endblock %}
