<form method="post" action="{{ path('add_dirigeants_post_delete', {'id': dirigeants_post.id}) }}" onsubmit="return confirm('Etez-vous sur de vouloir le supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dirigeants_post.id) }}">
    <button class="btn">Supprimer</button>
</form>
