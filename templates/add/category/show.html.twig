{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Categories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_category') }}">Retour à la liste</a>

    <a href="{{ path('add_category_edit', {'id': category.id}) }}">Editer</a>

    {{ include('add/category/_delete_form.html.twig') }}
{% endblock %}
