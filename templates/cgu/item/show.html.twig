{% extends 'base.html.twig' %}

{% block title %}Item{% endblock %}

{% block body %}
    <h1>la description de l'article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ item.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_cgu') }}">Retour à la liste</a>

    <a href="{{ path('item_edit', {'id': item.id}) }}">Editer</a>

    {{ include('cgu/item/_delete_form.html.twig') }}
{% endblock %}
