<form method="post" action="{{ path('cgu_delete', {'id': cgu.id}) }}" onsubmit="return confirm('Etez-vous sur de vouloir le supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cgu.id) }}">
    <button class="btn">Supprimer</button>
</form>
