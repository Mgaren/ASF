<form method="post" action="{{ path('basketball_planning_delete', {'id': basketball_planning.id}) }}" onsubmit="return confirm('Etez-vous sur de vouloir le supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ basketball_planning.id) }}">
    <button class="btn">Supprimer</button>
</form>
