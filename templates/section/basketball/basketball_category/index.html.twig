{% extends 'base.html.twig' %}

{% block title %}BasketballCategory index{% endblock %}

{% block body %}
    <h1>BasketballCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for basketball_category in basketball_categories %}
            <tr>
                <td>{{ basketball_category.id }}</td>
                <td>{{ basketball_category.name }}</td>
                <td>
                    <a href="{{ path('basketball_category_show', {'id': basketball_category.id}) }}">show</a>
                    <a href="{{ path('basketball_category_edit', {'id': basketball_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('basketball_category_new') }}">Create new</a>
{% endblock %}
