{% extends 'base.html.twig' %}

{% block title %}BasketballCategory{% endblock %}

{% block body %}
    <h1>BasketballCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ basketball_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ basketball_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_basketball_category') }}">Retour à la liste</a>

    <a href="{{ path('add_basketball_category_edit', {'id': basketball_category.id}) }}">Editer</a>

    {{ include('section/basketball/basketball_category/_delete_form.html.twig') }}
{% endblock %}
