{% extends 'base.html.twig' %}

{% block title %}BasketballCategory{% endblock %}

{% block body %}
    <h1>BasketballCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ section_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_section_category') }}">Retour à la liste</a>

    <a href="{{ path('add_section_category_edit', {'id': section_category.id}) }}">Editer</a>

    {# {{ include('section/section_category/_delete_form.html.twig') }} #}
{% endblock %}
