{% extends 'base.html.twig' %}

{% block title %}Section{% endblock %}

{% block body %}
    <h1>Section</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ section.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ section.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ section.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_homeSection') }}">Retour à la liste</a>

    <a href="{{ path('section_edit', {'id': section.id}) }}">Editer</a>

    {{ include('section/_delete_form.html.twig') }}
{% endblock %}
