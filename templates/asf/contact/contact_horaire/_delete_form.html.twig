<form method="post" action="{{ path('asf_contact_horaire_delete', {'id': contact_horaire.id}) }}" onsubmit="return confirm('Etez-vous sur de vouloir le supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_horaire.id) }}">
    <button class="btn">Supprimer</button>
</form>
