{% extends 'base.html.twig' %}

{% block title %}History{% endblock %}

{% block body %}
    <h1>History</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ history.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ history.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ history.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ history.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_history') }}">Retour à la liste</a>

    <a href="{{ path('asf_history_history_edit', {'id': history.id}) }}">Editer</a>

    {{ include('asf/history/_delete_form.html.twig') }}
{% endblock %}
